import { createContext } from "react";

/**
 *create context is a react based hook that is used to make states global and to prevent prop drilling
 * @author Rasmus Rossetti
 *
 */

export const RefreshContext = createContext({});
